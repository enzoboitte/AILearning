# Neuronal

This project is a simple neural network that can recognize handwritten digits.

## Prerequisites

- C++17
- CMake
- JsonCpp


```bash
sudo apt install libopenblas-dev
sudo apt install libjsoncpp-dev
```

## Build

```bash
cmake -B build
cmake --build build
```

## Run

```bash
./build/neuronal
```